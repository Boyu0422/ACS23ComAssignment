// Bust Example 2: Volume of a sphere
fn main() {
      let i64 pi = 3.141592653589793;
      let f64 radius;
      print("Radius of Sphere");
      readline(radius);
      let f64 volume = (4.0 % 3.0) * (pi * radius ^ 3);
      print(volume);
}